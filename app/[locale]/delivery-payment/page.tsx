import { Locale } from '@/lib/i18n'

export default function DeliveryPaymentPage({
  params,
}: {
  params: { locale: string }
}) {
  const locale = params.locale as Locale

  return (
    <div className="container py-12 max-w-3xl">
      <h1 className="text-4xl font-bold mb-8">
        {locale === 'uk' ? 'Доставка та оплата' : 'Доставка и оплата'}
      </h1>

      <div className="prose prose-gray max-w-none">
        <h2>{locale === 'uk' ? 'Доставка' : 'Доставка'}</h2>
        <p>
          {locale === 'uk'
            ? 'Ми відправляємо замовлення протягом 1-2 робочих днів після підтвердження.'
            : 'Мы отправляем заказы в течение 1-2 рабочих дней после подтверждения.'}
        </p>

        <h3>{locale === 'uk' ? 'Нова Пошта' : 'Новая Почта'}</h3>
        <p>
          {locale === 'uk'
            ? 'Доставка у відділення або поштомат. Вартість за тарифами перевізника.'
            : 'Доставка в отделение или почтомат. Стоимость по тарифам перевозчика.'}
        </p>

        <h3>{locale === 'uk' ? 'Укрпошта' : 'Укрпочта'}</h3>
        <p>
          {locale === 'uk'
            ? 'Доставка у відділення. Вартість за тарифами перевізника.'
            : 'Доставка в отделение. Стоимость по тарифам перевозчика.'}
        </p>

        <h2>{locale === 'uk' ? 'Оплата' : 'Оплата'}</h2>
        <p>
          {locale === 'uk'
            ? 'Оплата при отриманні накладеним платежем або на картку після узгодження з менеджером.'
            : 'Оплата при получении наложенным платежом или на карту после согласования с менеджером.'}
        </p>
      </div>
    </div>
  )
}
